{
  "name": "question_manager",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "client": "cd client && npm install && npm run build && serve -s build/",
    "server": "cd server && npm install && nodemon server.js",
    "start": "npm run server",
    "build": "npm run build"
  },
  "author": "tharmi",
  "license": "MIT"
}
